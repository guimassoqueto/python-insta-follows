```shell
make env
```

```shell
poetry shell
```

```shell
poetry install
```

```shell
pre-commit install && pre-commit autoupdate && pre-commit install --hook-type commit-msg
```

```shell
make a
```